# CLAUDE.md · options-desk（投资盘面）

> iOS 只读盘面。父级形态规则 `~/Apps/ios/CLAUDE.md`，建 app 的通用坑单 `/appios`，
> 全局偏好 `~/.claude/CLAUDE.md`。姊妹 app：`~/Apps/ios/options-calc`（期权决策台）。

**2026-08-28 立，2026-08-29 装上真机并铺开五页。** 口径三轮修完 → API 上线
（`desk.tianli.cyou`，authgate 闸内）→ iPhone 17 装机成功、实测取到闸内真数 → 五页全部接真端点。

> **凭证续期这条路已实测**（不是推断）：卸载重装清空容器 cookie 后，app 只凭 iOS 钥匙串里的密码
> 自己换了新会话 —— 服务端日志 `302 → /_gate/login 200 → POST /_gate/api/login 200 → /api/desk-summary 200`。
> 这条每 7 天真实走一次（闸 cookie `Max-Age=604800`），不验的话「点开就有数」只在头 7 天成立。

## 这个 app 回答什么

「我现在怎么样」：净值 · 累计收益 · **对 QQQ 的超额** · buffer · 六批票据梯子。

它**不**回答「如果我开这个结构会怎样」——那是 `options-calc`。两个刻意分开：
那个是纯函数计算器（零网络、零凭证、离线、3 个 Swift 文件，舰队里最稳的一个），
这个天天变、要联网要凭证要缓存要陈旧提示。合并会把最稳的变成最复杂的。

## 数据从哪来（**不造第二个账本**）

```
Robinhood MCP (get_*)
    → ~/investment/data/quant.db        rh_history / rh_group_pnl / rh_*_positions
    → review_run.py --facts             引擎值
    → 每日复盘正文 + 配图（blog-options）
```

**这条链是活的**（2026-08-28 查证：`rh_portfolios.asof` = 2026-08-27 16:00 EDT）。
本 app 是它的**消费端**。要新建的只有「把 quant.db 暴露成只读 API」，
而那个服务**也已经存在**：`~/Dev/stations/web-stack/services/stockoptions/api.py`，
`/api/equity-curve` `/api/summary` `/api/portfolio` 形状正好，只是数据源指着一堆
2026-06 就停更的 JSON。**重新指向 quant.db，禁新建第二个 API**（铁律 #5）。

## 三个目录各管一段（这不是一个目录的活）

| 目录 | 职责 |
|---|---|
| `~/investment/options/robinhood/` | 口径：回填 `qqq_close`、对账 `net_deposit`、把同步挂进 `review_run.py` |
| `~/Dev/stations/web-stack/services/stockoptions/` | 只读 API：数据源换成 quant.db，接 authgate |
| 本目录 | 客户端 |

## ⚠ 前身死过一次，死因写在这里

`cc-options` 曾经就是这个东西。2026-08-28 查证它的状态：

```
data/portfolio.json    停在 2026-06-01     LaunchAgent(17:00 同步)     没了
daily_nlv.csv          停在 2026-05-21     VPS 的 cc-options.service   没了
cc-options.tianli.cyou  DNS 不指向 VPS,也不在 menus 子域注册表里
```

> **2026-08-29 ssh 实测补充:比原来记的更彻底** —— VPS 上 `/var/www/web-stack` 整个目录不存在、
> 无任何 `tlz-*` systemd unit、无 nginx vhost、8621 无监听、`/var/www/cc-options/data` 缺失;
> configs repo 有一条 commit「退役 mindmap/n8n/stockoptions(2026-06-03 用户钦定砍单)」。
> 所以那个 API **不是「改数据源」就能用**,是「改源 + `/ship new` 四件全新
> (subdomains.yaml + CF DNS/Origin Rule + authgate + nginx vhost + systemd unit)」两件事。
> 顺带查实:`com.tianli.investment-eod-review.plist` 是**哑弹不是定时炸弹**
> (未加载、`~/Library/LaunchAgents/` 无副本、它指的 `eod_cron.sh` 根本不存在),已退役到 Trash。

**三个月里没有任何东西报过警。** 死因：同步挂在一个「Mac 得在 17:00 醒着」的
LaunchAgent 上，那个前提一破，整条链无声停摆。

由此两条硬约束，删一条这个 app 迟早重蹈覆辙：

1. **同步挂在用户每天真的会做的那件事上** —— `review_run.py`（写复盘必跑）顺带推
   quant.db。**不要再挂 LaunchAgent**：习惯不会静默死，cron 会。
2. **数据陈旧必须在界面上刺眼**，而不是安静地显示一个旧数字。
   同源教训：2026-08-28 blog-reader 的 blog-options 站因为撞访问闸静默贡献 0 篇，
   错误条上只有一句「解析失败」——既不像错误也指不出方向。

## 五个页面（2026-08-29 铺开）

一屏三个数字不够用 —— 用户原话「我怎么什么都看不了…得要可以点击的吧」。
`/appios` 那条「先跑一屏再铺开」是**门槛不是终点**：第一屏装进真机验过了，就该铺开。

| 页 | 端点 | 一句话 |
|---|---|---|
| 盘面 | `/api/desk-summary` | 累计收益 + 对 QQQ 超额 + 样本天数（不放年化，理由见下节） |
| **日志** | blog-options 的 `/feed.json` + `/api/post/<slug>`，加 `/api/orders?date=` | **每日操作记录**。一天两本账摞在一起：你写的复盘正文（markdown 真渲染）+ 当天真实成交 |
| 持仓 | `/api/portfolio` + `/api/roll-signals` | 票据梯子按到期日分批，每条腿带滚动建议；点开看希腊字母与理由 |
| 曲线 | `/api/twr` | **记分板**：我 vs QQQ，起点＝100。五档 `1周 / 1月 / 3月 / 锚日 / 全部`，默认锚日 |
| 风控 | `/api/exposure` · `/api/vol-trend` · `/api/harness` · `/api/perf-metrics` | **今天能拧的旋钮在前，已发生的记分在后**：当前暴露 → 20 日滚动波动 → 回撤硬约束（**当前破裂**）→ 绩效指标 |

**日志页不另造台账**（这条是硬约束）：每日复盘已经写在 blog-options 上了，83 篇一天一篇，
`series: daily-review`。app 读的就是那份，不是第二个输入框。多出来的价值只有一个 ——
把「我以为我做了什么」和「券商记的实际成交」摞在同一天上；分开看永远对不上。
`Feed.swift` 与 `MarkdownView.swift` 从 blog-reader 移植（舰队契约 6），只把站清单收窄到一个。

`DeskAPI` 是**取数唯一通路**：撞闸 → 换会话 → 重试一次，只此一份判据，五页共用。
各写一份的话，第一次改闸就会有几页悄悄坏掉、另几页还好着，而坏的那几页表现是「解码失败」。

### 曲线的锚日 = 2026-07-09（和博客记分板同一个锚）

锚来自 `~/investment/options/robinhood/race_series.py` 的 `RACE_ANCHOR`，那边注释写着
「写死，禁改 —— 改起点 ＝ 换了一个问题」，背后是 2026-08-12 一次真事故（起点被换成 08-03，
结论从「领先 +1.6 点」翻成「落后 0.74pp」并发布了）。所以 app 里档位是**显式五个**、
当前档起点一直显示在标题右边，不做成能悄悄改变结论的下拉框。

那 18 个「资金流未记录」的交易日全部 ≤ 2026-07-09，所以锚日档一直是干净的。
**2026-08-29 起全窗口都干净了**（见「那 18 天是怎么算出来的」），`excess_range` 因此不再
有值 —— 不是把不确定藏了，是它不成立了。区间机制留在 `api.py` 里，将来再出现定性不明的
资金流会自动重新给值。

> **和博客配图差 0.45 点，那是博客那边的 bug，不是这边的。** 2026-08-29 逐项拆解
> （主账户、从 07-09 起）：
>
> | 口径 | 我 | QQQ | 领先 | 链接 |
> |---|---|---|---|---|
> | A 博客原样（`substr(asof,1,10)` + `max()`，期末流） | +4.26% | −0.30% | **+4.56** | 33 |
> | B 只把归日换成 `session` | +3.81% | −0.30% | +4.11 | **35** |
> | C 再把同日取值换成最后一条 | +3.81% | −0.30% | +4.11 | 35 |
> | D 再换成期初流（＝app 口径） | +3.93% | −0.30% | **+4.23** | 35 |
>
> A 精确复现博客里那句「领先从 +4.56 退到 +4.43」，说明拆解是对的。差额几乎全部来自
> **归日**：全库 156 行有 44 行是收盘后甚至次日凌晨拉的快照，`substr` 归错天，
> 一条就抬高 0.45 点，同时吃掉 2 个交易日。**app 不照抄它** —— 为了和已发布的图对上
> 而退回一个已证实的错误，是这个项目最不该犯的那种错。
> `race_series.py` 未改（改了会让此前每一篇的图不可比；那是系列属性的决定，不是技术决定）。
>
> 另：博客图最右 08-28 的 QQQ 点用的是**当时尚未结算的盘中报价**，正是
> `backfill_qqq_close --repair` 明确挡在外面的那类值，所以 app 的窗口停在 08-27。

### 2026-08-29 补数据 + 停止「解释算不出什么」

用户两句话推的：「你都有 robinhood 的 mcp 了，就不能拉一整年的交易记录吗」「就我不想看到这些了」
「要做就做真实的，数据你也都有」。两件事分开：

**① 能拉的确实没拉。** `get_realized_pnl(span=all)` 一次掀底：账户有成交的月份回到 **2024-09**，
全时段 realized **$204,622**。而 `rh_orders` 从 2026-01-02 起 —— 那只是我们开始跑 dump 的日子，
不是账户的起点。已按 `created_at_gte` + `cursor` 拉满一年灌进库（复用现成的幂等 `append_orders`，
注入 `account_number`，那字段 MCP 不返、漏了会存成 `account=''` 让按账户过滤静默漏单）：

| 表 | 之前 | 现在 |
|---|---|---|
| `rh_orders` | 2026-01-02 起 1,022 单 | **2025-08-29 起 1,213 单** |
| `rh_equity_orders` | 43 行 | **2025-09-03 起 211 单** |

> MCP 超长响应会**自动落盘**到 `tool-results/`，不进上下文 —— 所以分页拉一年的成本只是
> 每页一次调用，不是把上千条带 legs 的 JSON 读进来。这是这条路可行的前提。

**② 拉不到的，别再反复解释。** 三件事的可拉性完全不同，别混着说：

| | MCP 能给吗 | 依据（schema 实读，不是推断） |
|---|---|---|
| 成交记录 | **能，到 2024-09** | `get_option_orders` / `get_equity_orders` 有 `created_at_gte` + `cursor` |
| 每日账户净值 | **不能** | `get_portfolio` 的参数**只有** `account_number`，无 span/interval |
| 资金流（入金/出金） | MCP **不能**，但**能反算** | MCP 无对应接口（四个候选 schema 逐个确认过）—— 但现金恒等式在补齐到期指派后闭合，资金流是**算出来的**。「拉不到」≠「得不到」，2026-08-29 前把这两件事混为一谈了 |

所以 TWR 样本长度是**结构性**的，补不了。但这不等于该在界面上反复道歉 ——
`_curve_metrics` 原来在 n<119 时把**六个指标全部**抹成 None，连
`total_return` / `max_drawdown`（**不含任何年化外推的路径统计量**）和 `annual_vol`
（σ 的相对标准误 ≈ 1/√(2n)，n=35 时约 12%，完全可用）也一起抹了。**这是过度拒绝。**
现在全都算，年化 / Sharpe 带 `se_*` 一起给。
**一个带误差带的数能拿来决策，一句「拒绝给出点估计」不能。**

**③ 资金流警告改成链接口径。** 真正污染收益率的是「链接」不是「天」——
窗口第一天的现金流只当分母基点，不进任何链接。锚日 2026-07-09 恰好是那种情况
（当天两条快照，日粒度取到的那条 `net_deposit` 是 NULL），报「1 天未知」会让人以为曲线不可信，
而受影响的链接是 **0** 个。服务端新增 `flow_unknown_links`，`provisional` 改由它驱动。
全窗口下照样报 17 个链接 —— **警告不是被藏起来了，是在它不成立的窗口里不再出现。**

`/api/harness` 同步加 `start=YYYY-MM-DD`（格式错 → 400，窗口内不足 2 个已结算 session → 503，
拒绝在空窗口上报绿）。⚠ 换窗口 = 换了一道题：锚日窗口下硬约束**照样是破的**
（我 −9.62% vs 加权基准 −7.17%，余量 −2.46%）—— 挑一个让破裂消失的窗口是作弊，不是修好。

### 调试用启动参数（只为截图存在）

| 参数 | 作用 |
|---|---|
| `-tab 0..4` | 起始页。没有它模拟器只能截到第一屏，另外四页就只能靠「编译过了」来相信 |
| `-openDay 2026-08-28` | 直接推开某天的日志详情（详情页是点进去才有的，截不到） |
| `-range w1｜m1｜m3｜anchor｜all` | 曲线档位。五档逐个截过，并用 Python 按同一规则独立复算逐位对过 |
| `-riskOnly exposure｜vol｜harness｜perf` | 风控页只渲一张卡。这页有四张卡、比一屏长得多，后两张在模拟器上根本截不到 —— 没有它，「下面那两张对不对」就只能靠「编译过了」来相信，而这个 app 栽过的坑全都编译得过 |

### 三个当场踩到、编译拦不住的坑

**① `/api/twr` 的四条序列是基数 100 的指数，不是分数。** 按分数处理画出的图**形状完全正常**，
只是数值写着「+12336%」。发现它靠的是把末值和 `/api/desk-summary` 的权威值对一下。
现在换算集中在 `CurveView.ret()` 一处，且**首值不是 100 就拒绝画图**并说明原因。

**② `/api/orders` 老版本会静默忽略 `date` 参数** —— 于是「当天成交」里躺着全库最近 200 笔，
顶上还写着「1,796 笔」，看着毫无破绽。服务端已加按天过滤（格式错 → 400，周日 → `count=0`，
两者分得开，响应 46KB → 5.8KB）；客户端加了**契约守卫**：响应不回显 `date` 就拒绝显示列表。

**③ 折线默认用了样条。** 样条会在两个真实收盘点之间画出数据里没有的起伏，1 周档只有 6 个点时
尤其明显。已改直线段 + 圆点（博客那张图也是这样）。同一轮还修了：选中态只靠颜色深浅时，
「选中的灰」和「未选中的灰」在截图里分不出来（QQQ 那条线本身是灰的）。

### 风控页的窗口从锚日改回全窗口（2026-08-29 二次订正）

当天早些时候把风控页统一到记分板锚日是对的 —— 那时全窗口只有 53 个 session 且有 18 天资金流未记录。
**这两条前提当天就没了**：序列补到 400 个 session、资金流全窗口逐日已知。
再拿 35 个链接算年化和 Sharpe 就是白扔九成样本。窗口口径的 SSOT 在 `DeskWindow`：
`raceAnchor` 给曲线（**系列属性**，要和已发布的图可比），`riskQuery` 给首屏与风控（要最长的干净样本）。

⚠ **换窗口不是为了让结论变好看**：回撤硬约束在两个窗口下都是破的
（全窗口 我 −32.11% vs 加权基准 −21.72%；锚日 −9.62% vs −7.17%）。没有能挑的绿窗口，也不去挑。

### 又三个编译拦不住的坑（2026-08-29 下半场，都是在模拟器上看出来的）

**④ 20 日滚动波动图上有个 75% 的假尖峰。** 它有形状、有持续 20 天的平台（窗口滑过去的过程），
**看起来完全像真的**。真身是 2026-05-21 → 06-09 那条跨 12 个交易日的链接被当成「一天」。
服务端加了 `_seam_links`（相邻 session 间隔 ≥ 3 个工作日即判为数据接缝；美股日历上连续两个工作日
假期基本不存在，所以这个阈值不会误伤假日），含接缝的窗口一律不出值。

**⑤ 挡掉尖峰之后，那个洞被 Charts 用一条笔直的斜线补上了 —— 图反而更「干净」。**
`compactMap` 掉 nil 再连成一条线，两端就会被直线相连，而那条线上每个像素都是数据里没有的东西。
改成**按空洞切段、每段一个 `series`**，断的地方真断，旁边一行说明为什么断。
判据是那句老话：**一个看得见的洞会让人去问为什么，一个假尖峰不会。**

**⑥ `Text("a" + "b")` 不渲染 markdown。** 实参类型是 `String` 就走
`Text(_: StringProtocol)` 那个重载，于是 `**路径事实**` 带着四个星号原样印在屏幕上
（截图实见）。只有**单个字面量**才会走 `Text(_: LocalizedStringKey)`。全局规矩「露出字面 `**` 即不合格」
在 SwiftUI 里的具体形态就是这一条。

## 首屏放什么（**不是年化**）

**2026-08-29 第三轮订正，前两版的数全部作废。** 口径：单账户 `5UK56277` · 逐日链接 TWR ·
当日现金流计入期初基数 · 序列 **2025-01-02 ~ 2026-08-27，400 个交易日 / 399 个链接**。

```
我        +30.69%
QQQ 同窗  +41.33%
★ 超额    -10.64pp        ← 是落后，不是领先
最大回撤  -32.11%（QQQ -22.88%）
资金流    全窗口逐日已知，0 个未知链接
```

**首屏第三块是「现在的风险」（2026-08-29 加）** —— 净暴露 0.77×NLV · 融资杠杆 2.50× ·
余量 1.7% · 20 日波动 我 13.5% vs QQQ 17.7%（自 07-30 的 33.1% 降下来）· 集中度 QQQ 占 93%。

> 加它的理由是用户当天定的新目标：「后面我会更加谨慎，精准，对冲，让自己波动少点」。
> **累计收益和超额是结果，今天做什么都改不了它们**；能动手的旋钮只有暴露和波动这两个。
> 首屏只印结果 = 每天盯着一个自己当下改不了的数。三个比率故意分开印，它们不是同一个问题：
> 融资口径（券商按它收保证金，2.50×）、方向口径（波动按它来，0.77×）、余量（它见底才真的动不了）——
> 只报融资口径像在说「你满仓杠杆」，只报方向口径像在说「你很轻」，两个都对。

> ### ⚠ 这一版把结论掀翻了：从「领先」变成「落后 10.6 个点」
>
> 上一版首屏是 **+46.48%**。差的那 15.8 个点不是算错，是**六月那 18 天的入金**原先没记，
> 被当成了投资赚来的钱。补上之后 2026-06-09~07-09 的净资金流是 **+$133,930**
> （06-09 −14,346 / 06-11 −50,025 / 06-18 +103,902 / 06-22 +21,372 / 06-30 +43,129 /
> 07-01 +29,898）。
>
> **一笔没记的入金错得毫无痕迹** —— 它不会让任何数字看起来异常，只会让收益率变好看。
> 这正是本文开头就写着要防的那类错，而它确实骗过了首屏三个月。

### 那 18 天是怎么算出来的（曾经被本文判为「无解，只能等」）

**判错的原因是找错了缺口。** 以为缺的是资金流的证据，实际缺的是**到期指派记录** ——
`rh_settlements` 最早的 expiry 是 2026-07-10，而账户从二月起每周都在被指派。06-30 那天
1400 股 QQQ 被 call 走进账 **+$1,007,800**，没有任何东西解释它，于是整笔被算进
「未解释残差」，残差大到没法当资金流读。

现在的链条（每一步都有反向验证，脚本在 `~/investment/options/robinhood/`）：

| 步 | 脚本 | 反向验证 |
|---|---|---|
| 从整年订单流重建持仓书 | `derive_expiry_settlements.py --verify-book` | 截到 08-28 与 `rh_option_positions` 真实快照逐条对：**13 条对 13 条，0 条不符** |
| 检出到期指派并定价 | 同上（价格源 = MCP `get_equity_historicals` 落的 CSV，脚本不联网） | 06-30 算出 +$1,007,800，与现金侧反推分毫不差 |
| 现金恒等式反算资金流 | `backfill_net_deposit_from_identity.py` | 18 天里 **12 天闭合到 ±$12**；`reconcile_net_deposit.py --daily` **判红 0 组** |

**两个数被券商自己的字段独立佐证**：06-30 的 +43,129 与 07-01 的 +29,898 —— 原始 dump 里
一直躺着 `pending_deposits`（07-01=43,132、07-02=73,032、07-09=0），只是从没被读进库。

**legacy 段拿来当过独立标尺**：2026-04-30 恒等式残差 +599,998.28 − 指派 +587,700 =
**+12,298**，而那天 legacy（另一套 b1_nlv 方法）记的入金是 **+12,299.97**；05-08 同法得
+10,013 vs +10,019.96。两套独立方法对到个位数 —— 这也是确认 legacy 行与 dump 行的
`cash` 是同一个口径的依据。

### 一个差点写进库的假入金（记在这里，因为它长得像真的）

第一版算出 06-09 那条链有 **+$269,654** 入金。它跨着 2026-05-22~06-08 那 12 天的快照空洞，
量级又和历史上的真实入金（2026-01-26 的 $180k、02-12 的 $204k）同级，**看起来完全合理**。

它是假的：05-21（legacy 最后一天）到期的那批票据指派现金合计 **+$284,000**，在 05-21
快照取值之后才入账，整笔落进了跨空洞那条链。证据是一对反号 —— 05-21 自己报 −$269,734、
06-09 报 +$269,654，**差 $80**。修掉后真实资金流是 **−$14,346**。

这条在**数据层**修掉（那几行 `settlement_date` 记 05-22、`note` 带 `seam:` 标记），
**不是在两个消费端各写一条特例**：先前那版在回填器里加了个 `basis` 特例，对账器不知道，
当场判红一条 +$284,000。同一条规则写两处 = 两个实现迟早分叉。

### 跨那 12 天空洞的 −19.5% 是真亏的（不是提现）

2026-05-22~06-08 没有任何快照，NLV 从 $1,240,990 掉到 $999,444。MCP 权威侧
（`get_realized_pnl`）那段实现亏损 **−$207,592**，NLV 掉 −$241,547，差额 −$33,954 是
未实现变动 + 费用 —— 量级正常，**那条链站得住**。

### 年化 / Sharpe 仍不放首屏，但理由变了

以前的理由是「样本只有 53 天」。补到 400 天之后那个理由不成立了 —— **算得出来，在风控页，
旁边印标准误**。首屏不放，是因为首屏只放三个数。

**超额是负的这件事也别过度解读**：399 个链接日里账户最大回撤 −32.1% 明显深于 QQQ 的
−21.7%，波动也更大。卖方结构在震荡市舒服、在单边上涨市跑输 —— 这段窗口正是它最难受的
regime，跟六月那 18 天的口径修正是两件事。

## 口径坑：七个已全修，只剩一个空洞（证据表已移走）

七类口径缺陷（`qqq_close` 缺一半 / 不是收盘价、归日错 28%、`net_deposit` 稀疏、
`rh_settlements` 缺 `settlement_date` 与六月及更早、`rh_orders` 疑似截断）**全部已修并反向验证过**，
逐条实测与修法见 `handoffs/data-truth-fix.md` §附。这里只留仍然成立的三条：

- ⛔ **2026-05-22~06-08 那 12 个交易日无解**（本机与远端都没有快照）。它现在是一条
  **13 个工作日的长链接**，带着 −18.31% 被当成「一天」—— 实测**它一条就占整段序列 29% 的方差**
  （σ 年化 27.2%，剔掉它 22.9%）。服务端已按 `SEAM_BIZ_DAYS` 识别它：20 日滚动波动图上
  含它的窗口一律不出值（线真断开，不是用直线补上），绩效表把它的方差占比印出来但**不改 σ**。
- ⚠ `fetch_data.append_history` 里 `dep = _f(...) or 0.0` —— dump 没带 `net_deposit` 时
  **写 0 而不是 NULL**，即「当天没有外部流水」被断言成事实而不是未知。多数天确实没流水，
  所以这个假设通常对；但它一旦错，错法和缺 NULL 一样无痕。**未改行为，记在这里。**
- `700013444` 的 `qqq_close` 全空不是缺口 —— 市场基准数不是账户属性，挂主账户一份就够。

## 已对齐的决策（2026-08-28，用户拍板）

- **新建本 app**，不并进 `options-calc`。
- **先修口径再建界面** —— 现在建，超额那栏一开始就是空的；而且 app 建完再改口径，两边都要动。
- ~~两个账户合并成一个总账~~ → **2026-08-29 用户钦定「3444 的账户就不要了」，改单账户
  `5UK56277`**。那个户 06-09 开、06-22 归零（此后 60 行 nlv 恒为 $0.06），整段生命都落在
  数据最脏的窗口里，却要求整本账为它保留一个「两笔划转是内部还是提现」的悬案。
  口径落在 `api.py` 的 `PORTFOLIO_ACCOUNTS`（一处白名单，五个端点共用；名单里的账户
  在库里不存在会当场 503，不会安静地少算一个还返回像全量的数）。

## 线上（2026-08-29 上线）

```
https://desk.tianli.cyou          authgate 闸内 · noindex · 不进任何导航
  /                               一页说明（/var/www/desk）
  /api/desk-summary               首屏（三个收益数 + risk 块；风险块算不出来时给 null + risk_error，
                                  **不降级成 0**，也不让首屏整个 503 —— 收益读序列，暴露读快照，两者无关）
  /api/exposure                   当前暴露：按标的的净 Δ / 融资杠杆 / 集中度 / 保证金余量
  /api/vol-trend?window=20        我 vs QQQ 的滚动年化波动序列（含 seam_links）
```

VPS 侧三件：`tlz-optionsdesk.service`（`uvicorn api:app` @ `127.0.0.1:8642`，User=www-data）·
vhost SSOT `~/Dev/tools/configs/nginx/vps/desk.tianli.cyou`（改完走 `nginx_ship`，**禁在 VPS 上直接改**）·
库副本 `/var/lib/tlz-optionsdesk/quant.db`。

**数据怎么上去的**：`review_run.py`（写复盘必跑）跑完顺带调 `push_quant_db.py` 推库
—— **不挂 LaunchAgent**，那正是前身的死因。推失败不拦复盘，只告警；
另一道是 app 首屏那条陈旧度条，几小时内自己变色。两道指向同一件事。

`/ship new` 的 `station_ship.py` **只做静态站**（rsync 一个含 index.html 的目录 + 静态 vhost），
所以这次是拆开走的：`ssot/dns/origin/access` 交给它，nginx vhost 与 systemd unit 按 VPS 上
现成的 `edu-points` 范式手落（仍走 `nginx_ship` 下发、仍登记 vhost 归属）。

> 路上修了总部一个真 bug：`cf_api.py origin-rules add` 用 `expr.rfind('"')` 定位插入点，
> 而那条规则的表达式尾部是 `... or starts_with(http.host, "app")` ——
> 新 host 被插进了 `starts_with` 的参数里，`starts_with(http.host, "app" "desk.tianli.cyou")`。
> 是 CF 报 400 才拦住的，**它的错误信息完全不提插错了位置**。已改成定位 `http.host in {…}` 集合本身。

## 每日自动更新（2026-08-29 立 —— 「app 得对着服务器实时更新」）

用户看到 08-28 没出结果。**根因不是 app，也不是账户数据缺**：08-28 的净值早在库里，
缺的只有那天的 QQQ/SPY/TQQQ 收盘价 —— dump 是收盘那一刻拉的，那时收盘 bar 还没结算，
回填器**拒绝**写未结算的价（这条规矩是对的），然后再没有任何东西回来补这一天。**每天都会复发。**

关键分界：**这两半的自动化难度完全不同**，混着谈就会得出「做不了」的错误结论。

| 数据 | 在哪自动 | 触发 | 凭证 |
|---|---|---|---|
| QQQ/SPY/TQQQ 收盘价 | **VPS**（公开行情） | `tlz-optionsdesk-bench.timer` 每小时 | 不需要 |
| 账户净值/持仓/成交 | **本机 Mac** | `com.tianli.optionsdesk-daily` 每小时 | 券商凭证，**不上 VPS** |

**基准那一半刻意不依赖 Mac 醒着** —— 挂在「Mac 醒着」上正是前身 cc-options 的死法。

```
VPS   bench_close_sync.py ──→ /var/lib/tlz-optionsdesk/bench.db   （单独文件！见下）
Mac   daily_auto.sh ─→ review_auto.py（缺件才跑）─→ push_quant_db.py ─→ VPS quant.db
API   _DAILY_SQL:  COALESCE(rh_history 的值, bench.db 的值)        ← 次序不能反
```

- **为什么 bench.db 是单独文件**：`push_quant_db.py` 是 **rsync 整个 quant.db**。往 VPS 上那个库里写任何东西，下一次推库都会整文件覆盖掉，而且**没有痕迹**（表还在、数没了）。
- **为什么 COALESCE 是「rh_history 优先」**：反过来的话，一次换源就会**改掉已经发布过的历史数字**。bench.db 只准往上**加** rh_history 缺的 session。四向实测过（挖洞库+bench → 兜住 401 天；挖洞库无 bench → 退回 400 天）。
- **换源前逐日验过**：Yahoo 日线 vs 库里 401 个 session × 3 个标的 = **1,203 次比较，0 处不符**（最大相对差 5.6e-8，纯浮点表示）；验证器反向测过（拿 SPY 的价对 QQQ 的列 → 判红 401 条）。
- **两个定时器都不挑「收盘后 N 分钟」**：挑时刻要跟 DST、半日市、机器睡没睡着较劲。两条链都幂等（当天那根 bar 一律不写；八件套已齐时 `review_auto.py` 是**真 no-op**，实测直接打印「无需跑」不起 claude 会话），所以多跑无代价、少跑下一小时补，机器睡醒第一次触发就补上。

> ⚠ **这两条链都可以死，而它们死掉这件事必须在首屏喊出来。** 守卫 = 陈旧度条，
> 2026-08-29 起按**交易日**算（服务端 `staleness_weekdays`）：0~1 新、2~3 报警、≥4 判死。
> 原来按自然日算，**每个周末都会假报警一次** —— 天天喊狼来了的警报等于没有警报，
> 而这条提示存在的全部理由就是补前身那个「三个月没人报警」的洞。
> 另一道：`/api/health` 的 `bench_db` 块（挂没挂上、多少行、最新 session、上次取数时间）。

文件：`~/Dev/stations/web-stack/services/stockoptions/{bench_close_sync.py,deploy/*.service,*.timer}` ·
`~/investment/options/robinhood/{daily_auto.sh,deploy/com.tianli.optionsdesk-daily.plist}` ·
日志 `~/Library/Logs/tlz-optionsdesk-daily.log` + 心跳 `.heartbeat.json`

## 凭证

复用 blog-reader 那套（2026-08-28 验过，零手工）：`Gate.swift` + `seed-gate.sh`，
密码存 **macOS 钥匙串**（`security -s tlz-gate`），装机时用 `-gatepw` 启动参数喂一次，
app 验过写进 iOS 钥匙串。

`Gate.swift` 从 blog-reader **移植不重写**（契约 6），只改 Keychain 的 service 标识。
撞闸的判据是**最终 URL 落在 `/_gate/` 下**，不是状态码 —— URLSession 会跟着 302，
到手的是登录页的 **200 HTML**，状态码分辨不出来；嗅 HTML 内容则是给页面文案建第二份判据，改个字就瞎。
`DeskError` 为此单列 `.gate` 一档：混进 `.decoding` 会显示成「数据对不上契约」，把人引向服务端去查。

## 选源（启动参数）

| 参数 | 源 |
|---|---|
| （默认） | `https://desk.tianli.cyou` —— 装到手机上点开就是真数 |
| `-apiBase <URL>` | 指别处，如本机调试 `-apiBase http://127.0.0.1:8799` |
| `-sample` | 本地样本，不联网（截图/离线调试用） |

> **密码禁进 `~/.personal_env`** —— 2026-08-28 实测 `XCBuildData` 会把构建时的完整环境
> **连值一起**记进中间产物（当时那里躺着 68 个真实凭证的明文）。
> 现在 HQ 的构建脚本已用 `scrub_env.sh` 摘掉，机器门 `secret_leak_audit.py`。

## 构建与装机

```bash
./sim-run.sh              # 模拟器（shim → 总部 SSOT）
./install-to-iphone.sh    # 真机（shim → 总部 SSOT）
```

付费 team `B9LJH93LA4`，证书 2027-08-28 到期。装机脚本尾部打印的到期日是从包里那张
`embedded.mobileprovision` 实读的，不是写死文案。

**先跑一屏再铺开**（`/appios` 硬约束，有 hook 实拦）：登录 + 一个主界面先装进模拟器看过，
才允许写第 3 个界面。导航范式错了是 N 处返工，不是 1 处。
